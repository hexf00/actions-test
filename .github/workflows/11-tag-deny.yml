name: 🍰 Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    if: ${{ !startsWith(github.ref, 'refs/tags/v') || !contains(github.ref, '-nightly.') }}
    runs-on: ubuntu-latest

    steps:
      - name: test
        run: echo "test"
